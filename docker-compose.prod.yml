services:
  web:
    ports: []
    build:
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    networks:
      - default
      - proxy_net

  api:
    networks:
      - default
      - proxy_net

  postgres:
    ports: []

networks:
  proxy_net:
    external: true
